# Duplicate Files Archive

This directory contains duplicate files and directories that were found in the repository with " 2" suffixes.

These duplicates were likely created accidentally during file system operations or sync issues.

## Contents

### Files
- Documentation duplicates (docs/* 2.md)
- Icon duplicates (icons/* 2.*)
- Source file duplicates (src/README 2.md)

### Directories
- `src/contexts 2/` - Duplicate contexts directory
- `src/offscreen 2/` - Duplicate offscreen directory
- `src/styles 2/` - Duplicate styles directory
- `src/test 2/` - Duplicate test directory
- `src/config 2/` - Duplicate config directory
- `src/background 2/` - Duplicate background directory
- `src/profile 2/` - Duplicate profile directory
- `e2e/helpers 2/` - Duplicate e2e helpers directory
- `e2e/tests 2/` - Duplicate e2e tests directory
- `.github/workflows 2/` - Duplicate workflows directory

## Note

These files are kept for reference but are not part of the active codebase. The original files (without " 2" suffix) are the canonical versions.

If you need to recover any content from these duplicates, they are preserved here.

